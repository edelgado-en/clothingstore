
/**
 * 
 * @param {*} state the redux store state test 
 * @returns the categories map from the categories reducer in the redux store
 */
export const getCategoriesMap = (state) => state.categories.categoriesMap;